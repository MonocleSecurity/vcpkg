diff --git a/include/boost/asio/ssl/impl/rfc2818_verification.ipp b/include/boost/asio/ssl/impl/rfc2818_verification.ipp
index 83e90c674..da5cd0172 100644
--- a/include/boost/asio/ssl/impl/rfc2818_verification.ipp
+++ b/include/boost/asio/ssl/impl/rfc2818_verification.ipp
@@ -104,7 +104,7 @@ bool rfc2818_verification::operator()(
 
   // No match in the alternate names, so try the common names. We should only
   // use the "most specific" common name, which is the last one in the list.
-  X509_NAME* name = X509_get_subject_name(cert);
+  X509_name_st* name = X509_get_subject_name(cert);
   int i = -1;
   ASN1_STRING* common_name = 0;
   while ((i = X509_NAME_get_index_by_NID(name, NID_commonName, i)) >= 0)
